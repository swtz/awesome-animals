// AULA 0308 | Transversing e Manipulação

// Transversing -> Navegar pela DOM

const lista = document.querySelector('.animais-lista');

lista.parentElement; // pai
lista.parentElement.parentElement; // pai do pai

lista.previousElementSibling; // elemento acima
lista.nextElementSibling; // elemento abaixo

lista.children; // HTMLCollection com os filhos
lista.children[0]; // primeiro filho
lista.children[--lista.children.length]; // último filho

lista.querySelectorAll('li'); // todas as LI's
lista.querySelector('li:last-child'); // último filho

// Element -> é um elemento html
// Node -> pode ser qualquer caractere presente no objeto document.documentElement

// métodos tais como 
HTMLElement.prototype.replaceChild()
HTMLElement.prototype.appendChild()
HTMLElement.prototype.firstChild
// lidam com os FILHOS do HTMLElement

HTMLElement.prototype.insertBefore()
// arg0: Elemento a ser movido, arg1: Elemento FILHO do HTMLElement


// Clonar Elementos
HTMLElement.prototype.cloneNode() // arg0: Boolean
// true -> inclui os filhos | false -> não inclui os filhos
// return -> new HTMLElement, ou seja, uma nova instância do elemento

// O default value desse método DEPENDENDO DO BROWSER é 'true'
